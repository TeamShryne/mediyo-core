# Workaround: rustc's default linker auto-detects `lld` from the system PATH
# (here: /opt/clang-18/bin/lld), which fails to link against the prebuilt std.
# Force the GNU bfd linker via gcc.
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "link-arg=-fuse-ld=bfd"]
